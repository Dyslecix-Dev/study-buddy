# Testing

## Commands

```bash
npm test                # Watch mode
npm run test:run        # Run once (before commit)
npm run test:ui         # Visual UI (recommended)
npm run test:coverage   # Coverage report
npm test gamification   # Specific tests
```

## Output

**Success:** `✓ Tests 28 passed (28)`

**Failure:** Shows expected vs received values

## Visual UI

`npm run test:ui` - Opens browser with interactive test interface

## Before Commit

`npm run test:run` - All tests must pass
